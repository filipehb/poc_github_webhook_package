{
  "name": "poc-github-webhook-package",
  "version": "1.0.0",
  "description": "GitHub package webhook listener that pulls container images and redeploys on DigitalOcean VPS",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
